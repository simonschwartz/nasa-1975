import { Meta, Story, Preview, Props, Test } from '@storybook/addon-docs/blocks';
import StemLogo from './';
import StoryContainer from '../../../.storybook/components/StoryContainer';

<Meta title="Stem Logo" component={StemLogo} />

# Stem Logo

The stem word allows the logotype to become both the major identification element and part of the
publication title.

This technique is used only on permanent, regularly published documents, and requires advice from
the NASA Graphics Coordinator at Headquarters.

<div style={{margin: '3rem 0'}}>
    <StemLogo title="Voyager" theme="dark" size="lg" style={{ marginBottom: '2rem' }} />
    <StemLogo title="Voyager" theme="dark" size="md" style={{ marginBottom: '2rem' }} />
    <StemLogo title="Voyager" theme="dark" size="sm" />
</div>

## Use of colour

<StoryContainer style={{ maxWidth: '28rem' }}>
    <StemLogo title="Voyager" theme="dark" size="sm" />
</StoryContainer>

<StoryContainer style={{ maxWidth: '28rem' }}>
    <StemLogo title="Voyager" theme="red" size="sm" />
</StoryContainer>

<StoryContainer background="dark" style={{ maxWidth: '28rem' }}>
    <StemLogo title="Voyager" theme="light" size="sm" />
</StoryContainer>


### API

```javascript
import { StemLogo } from 'nasa-1975';
```

<Props of={StemLogo} />

Any other properties supplied will be spread to the root element.
